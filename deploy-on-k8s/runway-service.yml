apiVersion: v1
kind: Service
metadata:
  labels:
    app: runaway
  name: runaway
  namespace: game
spec:
  type: NodePort
  ports:
  - name: "runway-service"
    port: 80
    targetPort: 80
    nodePort: 8080
  selector:
    app: runaway